{
  "name": "bloglist",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "build": "rm -rf build && cd ../bloglist-frontend && npm run build && cp -r dist ../blog-app/build",
    "deploy": "flyctl deploy",
    "deploy:full": "npm run build && npm run deploy",
    "logs:prod": "flyctl logs",
    "lint": "eslint .",
    "test": "NODE_ENV=test node --test",
    "start:test": "cross-env NODE_ENV=test node index.js"
  },
  "author": "Sam Williamson",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.7.0",
    "mongoose": "^8.4.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.7",
    "@stylistic/eslint-plugin-js": "^2.1.0",
    "eslint": "^9.4.0",
    "globals": "^15.4.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.3",
    "supertest": "^7.0.0"
  }
}
